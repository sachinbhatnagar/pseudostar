// Private validation data. Do not import from client modules.
export const internalSolutions: Record<string, string> = {
  "ref-1-1": "OUTPUT \"Enter a number between 1 and 10\"\nINPUT number\nIF number >= 1 AND number <= 10 THEN\n    OUTPUT \"That is correct\"\nELSE\n    OUTPUT \"That is incorrect\"\nENDIF",
  "ref-1-2": "OUTPUT \"Enter the first colour\"\nINPUT colour1\nOUTPUT \"Enter the first number or shape\"\nINPUT number1\nIF number1 == \"circle\" OR number1 == \"Circle\" THEN\n    number1 = 10\nELSEIF number1 == \"diamond\" OR number1 == \"Diamond\" THEN\n    number1 = 11\nELSEIF number1 == \"square\" OR number1 == \"Square\" THEN\n    number1 = 12\nENDIF\nOUTPUT \"Enter the second colour\"\nINPUT colour2\nOUTPUT \"Enter the second number or shape\"\nINPUT number2\nIF number2 == \"circle\" OR number2 == \"Circle\" THEN\n    number2 = 10\nELSEIF number2 == \"diamond\" OR number2 == \"Diamond\" THEN\n    number2 = 11\nELSEIF number2 == \"square\" OR number2 == \"Square\" THEN\n    number2 = 12\nENDIF\n\nIF colour1 == colour2 THEN\n    OUTPUT \"The cards have the same colour\"\nELSE\n    OUTPUT \"The cards have different colours\"\nENDIF\n\nIF number1 > number2 THEN\n    OUTPUT \"First number is larger\"\nELSEIF number2 > number1 THEN\n    OUTPUT \"Second number is larger\"\nELSE\n    OUTPUT \"The numbers are equal\"\nENDIF",
  "ref-1-3": "INPUT first\nINPUT second\nINPUT third\nIF first <= second AND first <= third THEN\n    result = second * third\nELSEIF second < third THEN\n    result = first * third\nELSE\n    result = first * second\nENDIF\nPRINT \"The result is \" + result",
  "ref-1-4": "OUTPUT \"Enter your name\"\nINPUT name\nOUTPUT \"Enter two numbers\"\nINPUT number1\nINPUT number2\nOUTPUT \"What is \", number1, \" + \", number2, \"?\"\nINPUT answer\ncorrect = 0\nIF answer == (number1 + number2) THEN\n    OUTPUT \"Correct\"\n    correct = correct + 1\nELSE\n    OUTPUT \"Incorrect\"\nENDIF\nOUTPUT \"What is \", number1, \" * \", number2, \"?\"\nINPUT answer\nIF answer == (number1 * number2) THEN\n    OUTPUT \"Correct\"\n    correct = correct + 1\nELSE\n    OUTPUT \"Incorrect\"\nENDIF\nOUTPUT name, \" you got \", correct, \" correct\"",
  "ref-1-5": "OUTPUT \"Enter the room type\"\nINPUT room\nOUTPUT \"Enter the number of nights\"\nINPUT nights\nIF room == \"elite\" OR room == \"Elite\" THEN\n    cost = 100 * nights\nELSE\n    cost = 50 * nights\nENDIF\nOUTPUT \"Are you a member?\"\nINPUT member\nIF member == \"Yes\" OR member == \"yes\" THEN\n    OUTPUT \"What level?\"\n    INPUT level\n    IF level == \"gold\" OR level == \"Gold\" THEN\n        cost = cost * 0.8\n    ELSEIF level == \"silver\" OR level == \"Silver\" THEN\n        cost = cost * 0.9\n    ELSEIF level == \"bronze\" OR level == \"Bronze\" THEN\n        cost = cost * 0.95\n    ELSE\n        OUTPUT \"Membership level not recognised\"\n    ENDIF\nENDIF\nOUTPUT \"Total cost is \", cost",
  "ref-1-6": "OUTPUT \"Enter the x-coordinate\"\nINPUT xCoord\nOUTPUT \"Enter the y-coordinate\"\nINPUT yCoord\nIF xCoord < 0 OR yCoord < 0 OR xCoord > 1024 OR yCoord > 1024\nTHEN\n    OUTPUT \"Your character is off the screen\"\nELSE\n    OUTPUT \"Your character is still on screen\"\nENDIF",
  "ref-1-7": "OUTPUT \"How much money do you have?\"\nINPUT money\nOUTPUT \"Do you have the Emerald Stone?\"\nINPUT stone\nOUTPUT \"What level are you on?\"\nINPUT level\nIF level < 3 THEN\n    OUTPUT \"Your level is not high enough yet\"\nELSEIF stone = \"no\" OR stone = \"NO\" OR stone = \"No\" THEN\n    OUTPUT \"You need to find the Emerald Stone\"\nELSEIF money < 100 THEN\n    OUTPUT \"You don't have enough money yet\"\nELSE\n    OUTPUT \"Congratulations, here is the Shield of Strength\"\nENDIF",
  "ref-2-1": "OUTPUT \"Enter a positive whole number\"\nINPUT limit\nFOR counter = 1 TO limit:\n    OUTPUT counter",
  "ref-2-2": "OUTPUT \"Enter a number\"\nINPUT number\nFOR counter = 1 TO 12\n    result = number * counter\n    OUTPUT number, \" x \", counter, \" = \", result\nNEXT counter",
  "ref-2-3": "total = 0\nFOR counter IN RANGE(1, 6):\n    OUTPUT \"Enter mark\"\n    INPUT mark\n    total = total + mark\naverage = total / 5\nOUTPUT \"The total is \", total\nOUTPUT \"The average is \", average",
  "ref-2-4": "evenCount = 0\noddCount = 0\nFOR counter = 1 TO 10\n    OUTPUT \"Enter a whole number\"\n    INPUT number\n    IF number MOD 2 == 0 THEN\n        evenCount = evenCount + 1\n    ELSE\n        oddCount = oddCount + 1\n    ENDIF\nNEXT counter\nOUTPUT \"Even numbers: \", evenCount\nOUTPUT \"Odd numbers: \", oddCount",
  "ref-2-5": "weeklyTotal = 0\nbusyDays = 0\nFOR day IN RANGE(0, 7):\n    OUTPUT \"Enter items sold\"\n    INPUT sold\n    weeklyTotal = weeklyTotal + sold\n    IF sold > 50 THEN\n        busyDays = busyDays + 1\n    ENDIF\nOUTPUT \"Weekly total is \", weeklyTotal\nOUTPUT \"Busy days: \", busyDays",
  "ref-3-1": "SUB-ROUTINE moveRight()\n    xCoord = xCoord + 1\n    IF xCoord > 1024 THEN\n        xCoord = 1024\n    ENDIF\nEND SUB\n\nINPUT xCoord\nINPUT yCoord\nOUTPUT \"Do you want to move right?\"\nINPUT choice\nIF choice = \"YES\" OR choice = \"yes\" OR choice = \"Yes\" THEN\n    moveRight()\nENDIF\nOUTPUT \"New position is x=\" & xCoord & \" y= \" & yCoord",
  "ref-3-2": "SUB-ROUTINE moveRight()\n    xCoord = xCoord + 1\n    IF xCoord > 1024 THEN\n        xCoord = 1024\n    ENDIF\nEND SUB\n\nSUB-ROUTINE moveLeft()\n    xCoord = xCoord - 1\n    IF xCoord < 0 THEN\n        xCoord = 0\n    ENDIF\nEND SUB\n\nSUB-ROUTINE moveUp()\n    yCoord = yCoord - 1\n    IF yCoord < 0 THEN\n        yCoord = 0\n    ENDIF\nEND SUB\n\nSUB-ROUTINE moveDown()\n    yCoord = yCoord + 1\n    IF yCoord > 1024 THEN\n        yCoord = 1024\n    ENDIF\nEND SUB\n\nINPUT xCoord\nINPUT yCoord\nOUTPUT \"Enter a direction\"\nINPUT direction\nIF direction = \"right\" OR direction = \"Right\" THEN\n    moveRight()\nELSEIF direction = \"left\" OR direction = \"Left\" THEN\n    moveLeft()\nELSEIF direction = \"up\" OR direction = \"Up\" THEN\n    moveUp()\nELSEIF direction = \"down\" OR direction = \"Down\" THEN\n    moveDown()\nELSE\n    OUTPUT \"Direction not recognised\"\nENDIF\nOUTPUT \"New position is x=\" & xCoord & \" y= \" & yCoord",
  "ref-3-3": "SUB-ROUTINE takeDamage()\n    OUTPUT \"Enter damage\"\n    INPUT damage\n    health = health - damage\n    IF health < 0 THEN\n        health = 0\n    ENDIF\nEND SUB\n\nSUB-ROUTINE heal()\n    OUTPUT \"Enter heal amount\"\n    INPUT amount\n    health = health + amount\n    IF health > 100 THEN\n        health = 100\n    ENDIF\nEND SUB\n\nSUB-ROUTINE showHealth()\n    OUTPUT \"Health is \" & health\nEND SUB\n\nhealth = 80\nOUTPUT \"Enter attack or heal\"\nINPUT choice\nIF choice = \"attack\" OR choice = \"Attack\" THEN\n    takeDamage()\nELSEIF choice = \"heal\" OR choice = \"Heal\" THEN\n    heal()\nELSE\n    OUTPUT \"Choice not recognised\"\nENDIF\nshowHealth()",
  "ref-3-4": "SUB-ROUTINE setRoomCost()\n    OUTPUT \"Enter the room type\"\n    INPUT room\n    OUTPUT \"Enter the number of nights\"\n    INPUT nights\n    IF room = \"elite\" OR room = \"Elite\" THEN\n        cost = 100 * nights\n    ELSE\n        cost = 50 * nights\n    ENDIF\nEND SUB\n\nSUB-ROUTINE applyDiscount()\n    OUTPUT \"What level?\"\n    INPUT level\n    IF level = \"gold\" OR level = \"Gold\" THEN\n        cost = cost * 0.8\n    ELSEIF level = \"silver\" OR level = \"Silver\" THEN\n        cost = cost * 0.9\n    ELSEIF level = \"bronze\" OR level = \"Bronze\" THEN\n        cost = cost * 0.95\n    ELSE\n        OUTPUT \"Membership level not recognised\"\n    ENDIF\nEND SUB\n\nSUB-ROUTINE showTotal()\n    OUTPUT \"Total cost is \" & cost\nEND SUB\n\nsetRoomCost()\nOUTPUT \"Are you a member?\"\nINPUT member\nIF member = \"Yes\" OR member = \"yes\" THEN\n    applyDiscount()\nENDIF\nshowTotal()",
  "ref-3-5": "SUB-ROUTINE checkPurchase()\n    OUTPUT \"How much money do you have?\"\n    INPUT money\n    OUTPUT \"Do you have the Emerald Stone?\"\n    INPUT stone\n    OUTPUT \"What level are you on?\"\n    INPUT level\n    IF level < 3 THEN\n        OUTPUT \"Your level is not high enough yet\"\n    ELSEIF stone = \"no\" OR stone = \"NO\" OR stone = \"No\" THEN\n        OUTPUT \"You need to find the Emerald Stone\"\n    ELSEIF money < 100 THEN\n        OUTPUT \"You don't have enough money yet\"\n    ELSE\n        OUTPUT \"Congratulations, here is the Shield of Strength\"\n    ENDIF\nEND SUB\n\ncheckPurchase()",
  "ref-3-6": "SUB-ROUTINE askQuestion()\n    OUTPUT \"What is \" & number1 & \" * \" & number2 & \"?\"\n    INPUT answer\n    IF answer = (number1 * number2) THEN\n        OUTPUT \"Correct\"\n        score = score + 1\n    ELSE\n        OUTPUT \"Incorrect\"\n    ENDIF\nEND SUB\n\nnumber1 = 7\nnumber2 = 8\nscore = 0\nFOR counter = 1 TO 5\n    askQuestion()\nNEXT counter\nOUTPUT \"Final score is \" & score",
  "ref-3-7": "SUB-ROUTINE checkPassword()\n    OUTPUT \"Enter the password\"\n    INPUT attempt\n    IF attempt = password THEN\n        unlocked = \"yes\"\n    ELSE\n        unlocked = \"no\"\n    ENDIF\nEND SUB\n\nSUB-ROUTINE showResult()\n    IF unlocked = \"yes\" THEN\n        OUTPUT \"Access granted\"\n    ELSE\n        OUTPUT \"Access denied\"\n    ENDIF\nEND SUB\n\npassword = \"LOCKED\"\nunlocked = \"no\"\nFOR counter = 1 TO 3\n    IF unlocked = \"no\" THEN\n        checkPassword()\n    ENDIF\nNEXT counter\nshowResult()",
  "ref-3-8": "SUB-ROUTINE attack()\n    IF energy < 10 THEN\n        OUTPUT \"Not enough energy\"\n    ELSE\n        energy = energy - 10\n        enemyHealth = enemyHealth - 15\n        IF enemyHealth < 0 THEN\n            enemyHealth = 0\n        ENDIF\n    ENDIF\nEND SUB\n\nSUB-ROUTINE rest()\n    energy = energy + 10\n    IF energy > 50 THEN\n        energy = 50\n    ENDIF\nEND SUB\n\nSUB-ROUTINE showStatus()\n    OUTPUT \"Health is \" & health\n    OUTPUT \"Energy is \" & energy\n    OUTPUT \"Enemy health is \" & enemyHealth\nEND SUB\n\nhealth = 100\nenergy = 50\nenemyHealth = 60\nFOR turn = 1 TO 4\n    OUTPUT \"Enter attack or rest\"\n    INPUT choice\n    IF choice = \"attack\" OR choice = \"Attack\" THEN\n        attack()\n    ELSEIF choice = \"rest\" OR choice = \"Rest\" THEN\n        rest()\n    ELSE\n        OUTPUT \"Choice not recognised\"\n    ENDIF\n    showStatus()\nNEXT turn",
  "snack-bill": "INPUT sandwiches\nINPUT juices\ntotal = sandwiches * 4 + juices * 3\nOUTPUT \"Bill: \", total",
  "clock-split": "INPUT duration\nminutes = duration MOD 60\nhours = (duration - minutes) / 60\nOUTPUT \"Hours: \", hours\nOUTPUT \"Minutes: \", minutes",
  "equal-shares": "INPUT counters\nINPUT players\nleft = counters MOD players\neach = (counters - left) / players\nOUTPUT \"Each: \", each\nOUTPUT \"Left: \", left",
  "garden-fence": "INPUT length\nINPUT width\nINPUT gate\nfence = (length + width) * 2 - gate\nOUTPUT \"Fence: \", fence",
  "temperature-scale": "INPUT celsius\nfahrenheit = celsius * 9 / 5 + 32\nOUTPUT \"Fahrenheit: \", fahrenheit",
  "midpoint": "INPUT x1\nINPUT y1\nINPUT x2\nINPUT y2\nOUTPUT \"Midpoint x: \", (x1 + x2) / 2\nOUTPUT \"Midpoint y: \", (y1 + y2) / 2",
  "track-distance": "INPUT first\nINPUT second\nIF first >= second THEN\n    distance = first - second\nELSE\n    distance = second - first\nENDIF\nOUTPUT \"Distance: \", distance",
  "cash-desk": "INPUT bill\nINPUT payment\nIF payment >= bill THEN\n    OUTPUT \"Change: \", payment - bill\nELSE\n    OUTPUT \"Still due: \", bill - payment\nENDIF",
  "parcel-charge": "INPUT weight\nIF weight <= 2 THEN\n    cost = 5\nELSE\n    cost = 5 + (weight - 2) * 3\nENDIF\nOUTPUT \"Charge: \", cost",
  "case-sensitive-code": "INPUT code\nIF code = \"BlueDoor\" THEN\n    OUTPUT \"Open\"\nELSE\n    OUTPUT \"Closed\"\nENDIF",
  "swap-labels": "INPUT first\nINPUT second\ntemporary = first\nfirst = second\nsecond = temporary\nOUTPUT \"First: \", first\nOUTPUT \"Second: \", second",
  "route-length": "INPUT kilometres\nINPUT metres\nINPUT detour\ntotal = kilometres * 1000 + metres + detour\nOUTPUT \"Metres: \", total",
  "launch-countdown": "INPUT start\nFOR counter = 1 TO start\n    OUTPUT start - counter + 1\nNEXT counter\nOUTPUT \"Launch\"",
  "shelf-labels": "INPUT first\nINPUT last\nFOR shelf = first TO last\n    OUTPUT \"Shelf \", shelf\nNEXT shelf\nOUTPUT \"Labels complete\"",
  "square-sizes": "INPUT maximum\nFOR side = 1 TO maximum\n    OUTPUT \"Side \", side, \": \", side * side\nNEXT side",
  "even-labels": "INPUT limit\nFOR number = 1 TO limit\n    IF number MOD 2 = 0 THEN\n        OUTPUT number\n    ENDIF\nNEXT number\nOUTPUT \"Done\"",
  "valid-mark-average": "valid = 0\ntotal = 0\nFOR i = 1 TO 5\n    INPUT mark\n    IF mark >= 0 AND mark <= 100 THEN\n        valid = valid + 1\n        total = total + mark\n    ENDIF\nNEXT i\nOUTPUT \"Valid: \", valid\nIF valid > 0 THEN\n    OUTPUT \"Average: \", total / valid\nELSE\n    OUTPUT \"No valid marks\"\nENDIF",
  "reading-streak": "current = 0\nlongest = 0\nFOR day = 1 TO 7\n    INPUT minutes\n    IF minutes >= 20 THEN\n        current = current + 1\n        IF current > longest THEN\n            longest = current\n        ENDIF\n    ELSE\n        current = 0\n    ENDIF\nNEXT day\nOUTPUT \"Longest streak: \", longest",
  "sensor-spread": "INPUT count\nINPUT reading\nminimum = reading\nmaximum = reading\nFOR i = 2 TO count\n    INPUT reading\n    IF reading < minimum THEN\n        minimum = reading\n    ENDIF\n    IF reading > maximum THEN\n        maximum = reading\n    ENDIF\nNEXT i\nOUTPUT \"Minimum: \", minimum\nOUTPUT \"Maximum: \", maximum\nOUTPUT \"Spread: \", maximum - minimum",
  "water-tariff": "INPUT units\nIF units <= 10 THEN\n    cost = units * 2\nELSEIF units <= 20 THEN\n    cost = 20 + (units - 10) * 3\nELSE\n    cost = 50 + (units - 20) * 5\nENDIF\nOUTPUT \"Water bill: \", cost",
  "triangle-kind": "INPUT a\nINPUT b\nINPUT c\nIF a <= 0 OR b <= 0 OR c <= 0 OR a + b <= c OR a + c <= b OR b + c <= a THEN\n    OUTPUT \"Invalid\"\nELSEIF a = b AND b = c THEN\n    OUTPUT \"Equilateral\"\nELSEIF a = b OR a = c OR b = c THEN\n    OUTPUT \"Isosceles\"\nELSE\n    OUTPUT \"Scalene\"\nENDIF",
  "overnight-duration": "INPUT startHour\nINPUT startMinute\nINPUT endHour\nINPUT endMinute\nduration = endHour * 60 + endMinute - startHour * 60 - startMinute\nIF duration < 0 THEN\n    duration = duration + 1440\nENDIF\nOUTPUT \"Duration: \", duration",
  "wallet-ledger": "INPUT balance\nFOR i = 1 TO 4\n    INPUT amount\n    IF balance + amount < 0 THEN\n        OUTPUT \"Rejected\"\n    ELSE\n        balance = balance + amount\n        OUTPUT \"Accepted\"\n    ENDIF\nNEXT i\nOUTPUT \"Balance: \", balance",
  "earliest-bid": "INPUT count\nINPUT bid\nbest = bid\nwinner = 1\nFOR bidder = 2 TO count\n    INPUT bid\n    IF bid > best THEN\n        best = bid\n        winner = bidder\n    ENDIF\nNEXT bidder\nOUTPUT \"Winner: \", winner\nOUTPUT \"Bid: \", best",
  "alternating-score": "INPUT count\nscore = 0\nFOR position = 1 TO count\n    INPUT number\n    IF position MOD 2 = 1 THEN\n        score = score + number\n    ELSE\n        score = score - number\n    ENDIF\nNEXT position\nOUTPUT \"Score: \", score",
  "saving-target": "INPUT target\nsaved = 0\nfirstDay = 0\nFOR day = 1 TO 7\n    INPUT deposit\n    saved = saved + deposit\n    IF saved >= target AND firstDay = 0 THEN\n        firstDay = day\n    ENDIF\nNEXT day\nIF firstDay = 0 THEN\n    OUTPUT \"Target not reached\"\nELSE\n    OUTPUT \"Reached on day \", firstDay\nENDIF",
  "ballot-count": "aCount = 0\nbCount = 0\ninvalid = 0\nFOR i = 1 TO 6\n    INPUT vote\n    IF vote = \"a\" THEN\n        aCount = aCount + 1\n    ELSEIF vote = \"b\" THEN\n        bCount = bCount + 1\n    ELSE\n        invalid = invalid + 1\n    ENDIF\nNEXT i\nOUTPUT \"A: \", aCount\nOUTPUT \"B: \", bCount\nOUTPUT \"Invalid: \", invalid\nIF aCount > bCount THEN\n    OUTPUT \"Winner: A\"\nELSEIF bCount > aCount THEN\n    OUTPUT \"Winner: B\"\nELSE\n    OUTPUT \"Tie\"\nENDIF",
  "second-chance": "INPUT first\nINPUT second\nINPUT answer\nIF answer = first * second THEN\n    points = 2\nELSE\n    INPUT answer\n    IF answer = first * second THEN\n        points = 1\n    ELSE\n        points = 0\n    ENDIF\nENDIF\nOUTPUT \"Points: \", points",
  "obstacle-robot": "SUB-ROUTINE proposeMove()\n    nextX = x\n    nextY = y\n    known = 1\n    IF direction = \"right\" THEN\n        nextX = x + 1\n    ELSEIF direction = \"left\" THEN\n        nextX = x - 1\n    ELSEIF direction = \"up\" THEN\n        nextY = y - 1\n    ELSEIF direction = \"down\" THEN\n        nextY = y + 1\n    ELSE\n        known = 0\n    ENDIF\nEND SUB\nSUB-ROUTINE commitMove()\n    IF known = 0 OR nextX < 0 OR nextX > 4 OR nextY < 0 OR nextY > 4 THEN\n        OUTPUT \"Blocked\"\n    ELSEIF nextX = 2 AND nextY = 2 THEN\n        OUTPUT \"Blocked\"\n    ELSE\n        x = nextX\n        y = nextY\n        OUTPUT \"Moved\"\n    ENDIF\nEND SUB\nx = 1\ny = 1\nFOR turn = 1 TO 5\n    INPUT direction\n    proposeMove()\n    commitMove()\nNEXT turn\nOUTPUT \"Position: \", x, \",\", y",
  "tank-accounting": "SUB-ROUTINE fillTank()\n    water = water + amount\n    IF water > 100 THEN\n        spilled = spilled + water - 100\n        water = 100\n    ENDIF\nEND SUB\nSUB-ROUTINE drainTank()\n    water = water - amount\n    IF water < 0 THEN\n        unmet = unmet - water\n        water = 0\n    ENDIF\nEND SUB\nwater = 30\nspilled = 0\nunmet = 0\nFOR operation = 1 TO 4\n    INPUT action\n    INPUT amount\n    IF action = \"fill\" THEN\n        fillTank()\n    ELSE\n        drainTank()\n    ENDIF\nNEXT operation\nOUTPUT \"Water: \", water\nOUTPUT \"Spilled: \", spilled\nOUTPUT \"Unmet: \", unmet",
  "ticket-kiosk": "SUB-ROUTINE buyTickets()\n    IF quantity > stock THEN\n        OUTPUT \"Sold out\"\n    ELSEIF quantity * 4 > credit THEN\n        OUTPUT \"Need credit\"\n    ELSE\n        stock = stock - quantity\n        credit = credit - quantity * 4\n        OUTPUT \"Purchased\"\n    ENDIF\nEND SUB\nSUB-ROUTINE topUp()\n    credit = credit + quantity\n    OUTPUT \"Credit added\"\nEND SUB\nstock = 5\ncredit = 12\nFOR i = 1 TO 4\n    INPUT action\n    INPUT quantity\n    IF action = \"buy\" THEN\n        buyTickets()\n    ELSE\n        topUp()\n    ENDIF\nNEXT i\nOUTPUT \"Stock: \", stock\nOUTPUT \"Credit: \", credit",
  "quiz-streak-bonus": "SUB-ROUTINE askQuestion()\n    INPUT first\n    INPUT second\n    INPUT answer\n    IF answer = first * second THEN\n        score = score + 2\n        IF streak > 0 THEN\n            score = score + 1\n        ENDIF\n        streak = streak + 1\n        OUTPUT \"Correct\"\n    ELSE\n        streak = 0\n        OUTPUT \"Incorrect\"\n    ENDIF\nEND SUB\nscore = 0\nstreak = 0\nFOR question = 1 TO 4\n    askQuestion()\nNEXT question\nOUTPUT \"Score: \", score",
  "checkpoint-run": "SUB-ROUTINE advanceRunner()\n    oldPosition = position\n    position = position + amount\n    IF position > 30 THEN\n        position = 30\n    ENDIF\n    FOR checkpoint = 1 TO 3\n        threshold = checkpoint * 10\n        IF oldPosition < threshold AND position >= threshold THEN\n            checkpoints = checkpoints + 1\n        ENDIF\n    NEXT checkpoint\nEND SUB\nposition = 0\ncheckpoints = 0\nFOR turn = 1 TO 4\n    INPUT amount\n    advanceRunner()\n    OUTPUT \"Position: \", position\n    OUTPUT \"Checkpoints: \", checkpoints\nNEXT turn",
  "access-pair": "SUB-ROUTINE checkAccess()\n    INPUT name\n    INPUT code\n    attempts = attempts + 1\n    IF name = \"guide\" AND code = \"MAP\" THEN\n        granted = \"yes\"\n    ENDIF\nEND SUB\ngranted = \"no\"\nattempts = 0\nFOR turn = 1 TO 3\n    IF granted = \"no\" THEN\n        checkAccess()\n    ENDIF\nNEXT turn\nIF granted = \"yes\" THEN\n    OUTPUT \"Access granted\"\nELSE\n    OUTPUT \"Access denied\"\nENDIF\nOUTPUT \"Attempts: \", attempts",
  "tournament-totals": "SUB-ROUTINE readTeamTotal()\n    total = 0\n    FOR round = 1 TO 2\n        INPUT points\n        total = total + points\n    NEXT round\nEND SUB\nbest = -1\nleaders = 0\nFOR team = 1 TO 3\n    readTeamTotal()\n    OUTPUT \"Team \", team, \": \", total\n    IF total > best THEN\n        best = total\n        leaders = 1\n    ELSEIF total = best THEN\n        leaders = leaders + 1\n    ENDIF\nNEXT team\nOUTPUT \"Best: \", best\nOUTPUT \"Leaders: \", leaders",
  "kit-orders": "SUB-ROUTINE fulfilOrder()\n    INPUT needWheels\n    INPUT needFrames\n    IF needWheels <= wheels AND needFrames <= frames THEN\n        wheels = wheels - needWheels\n        frames = frames - needFrames\n        OUTPUT \"Accepted\"\n    ELSE\n        OUTPUT \"Rejected\"\n    ENDIF\nEND SUB\nwheels = 12\nframes = 8\nFOR order = 1 TO 4\n    fulfilOrder()\nNEXT order\nOUTPUT \"Wheels: \", wheels\nOUTPUT \"Frames: \", frames",
  "thermostat-memory": "SUB-ROUTINE updateHeater()\n    IF temperature < 18 AND heater = \"off\" THEN\n        heater = \"on\"\n        switches = switches + 1\n    ELSEIF temperature > 22 AND heater = \"on\" THEN\n        heater = \"off\"\n        switches = switches + 1\n    ENDIF\nEND SUB\nheater = \"off\"\nswitches = 0\nFOR reading = 1 TO 6\n    INPUT temperature\n    updateHeater()\n    OUTPUT \"Heater: \", heater\nNEXT reading\nOUTPUT \"Switches: \", switches",
  "single-undo": "SUB-ROUTINE addScore()\n    INPUT amount\n    previous = score\n    score = score + amount\n    canUndo = \"yes\"\nEND SUB\nSUB-ROUTINE undoScore()\n    IF canUndo = \"yes\" THEN\n        score = previous\n        canUndo = \"no\"\n    ENDIF\nEND SUB\nscore = 0\ncanUndo = \"no\"\nFOR turn = 1 TO 5\n    INPUT command\n    IF command = \"add\" THEN\n        addScore()\n    ELSEIF command = \"undo\" THEN\n        undoScore()\n    ELSE\n        score = 0\n        canUndo = \"no\"\n    ENDIF\n    OUTPUT \"Score: \", score\nNEXT turn",
  "delivery-batch": "SUB-ROUTINE calculateOrder()\n    INPUT count\n    cost = 0\n    FOR item = 1 TO count\n        INPUT price\n        cost = cost + price\n    NEXT item\n    INPUT member\n    IF count > 0 THEN\n        IF member = \"yes\" THEN\n            cost = cost * 0.9\n        ENDIF\n        IF cost < 50 THEN\n            cost = cost + 5\n        ENDIF\n    ENDIF\nEND SUB\nbatch = 0\nFOR order = 1 TO 3\n    calculateOrder()\n    OUTPUT \"Order \", order, \": \", cost\n    batch = batch + cost\nNEXT order\nOUTPUT \"Batch: \", batch",
  "divisor-survey": "SUB-ROUTINE countDivisors()\n    divisors = 0\n    FOR candidate = 1 TO number\n        IF number MOD candidate = 0 THEN\n            divisors = divisors + 1\n        ENDIF\n    NEXT candidate\nEND SUB\nprimes = 0\nFOR query = 1 TO 3\n    INPUT number\n    countDivisors()\n    OUTPUT \"Divisors of \", number, \": \", divisors\n    IF divisors = 2 THEN\n        primes = primes + 1\n    ENDIF\nNEXT query\nOUTPUT \"Primes: \", primes"
};
